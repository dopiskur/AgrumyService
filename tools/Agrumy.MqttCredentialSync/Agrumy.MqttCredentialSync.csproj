<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>Agrumy.MqttCredentialSync</RootNamespace>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <!-- Only Agrumy.Dal is needed (DbOptionsFactory/AgrumyDbContext) - reads device/tenant-quota rows directly, no Agrumy.Api web host involved. -->
  <ItemGroup>
    <ProjectReference Include="..\..\Agrumy.Dal\Agrumy.Dal.csproj" />
  </ItemGroup>

</Project>
